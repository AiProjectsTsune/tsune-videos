# tsune-videos 引き継ぎ

- GitHub Pages の動画紹介ページ。配信ファイルは `index.html`。
- 生成元は `../sns/src/content/video_promo/link_in_bio.py`。変更する場合は sns の AGENTS.md を先に読む。
- 確認日 2026-09-10。開始時 HEAD `d9b53db`、main、作業ツリーは clean、origin あり。
- このフォルダに旧 CLAUDE.md・専用メモリは未検出。共通メモリは、プロジェクトルート基準で `../doc/CODEX_HANDOFF.md` が存在する場合に参照。
- 生成スクリプトには push・公開処理が含まれる。文書の確認だけで実行しない。作業ブランチへの push と公開ブランチへの反映を区別する。
- 今回は Codex 用文書のみ追加。公開ページ・生成処理は変更していない。
